# Server-only. Set these on Vercel → Settings → Environment Variables.
# Never prefix STRIPE_SECRET_KEY with VITE_ (that would expose it in the browser).
STRIPE_SECRET_KEY=
PUBLIC_SITE_URL=https://www.proximatedate.com

# Static SPA for Cloudflare Workers Builds.
# Production checkout still runs on Vercel (`/api/create-checkout`).
# Dashboard build command should be: npm run build
name = "proximatedate"
compatibility_date = "2026-08-27"
workers_dev = true

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
